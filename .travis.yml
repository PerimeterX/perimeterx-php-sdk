language: php
sudo: false
php:
  - 5.5
  - 5.6
  - 7.0
  - nightly

install:
  - composer global require "phpunit/phpunit=4.8.*"

before_script:
  - mv tests/bootstrap.php.dist tests/bootstrap.php
  - mv phpunit.xml.dist phpunit.xml
  - composer install

script: phpunit tests/PerimeterxCookieValidatorTest
